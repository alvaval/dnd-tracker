import CombatOverview from "@/components/CombatOverview";

export default function CombatPage() {
  return (
      <CombatOverview />
      // Maybe later make more components for combat, or create two different views? One for DM, one for players
  );
}